# Reversion Notice

The Unity frame timing visualizer introduced in commit 299a1e4a was removed at the user's request. The latest change restores the repository to its state before that visualization was added.
